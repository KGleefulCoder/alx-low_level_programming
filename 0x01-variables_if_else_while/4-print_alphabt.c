#include<stdio.h>
/**
 * main - main block
 * Description: Print an alphabet in lowercase, followed by a new line.
 * Return: 0
 */
int main(void)
{
	char ='a';
	while (c <= 'z')
	{
		if (c != 'q' && c != 'e')
			putchar(c);
		c++
	}
	putchar('\n');
	return (0);
}
